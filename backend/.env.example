# MongoDB Connection URI
# For local MongoDB: mongodb://localhost:27017/live-polling
# For MongoDB Atlas: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/live-polling

# Server Port (optional, defaults to 5000)
PORT=5000
